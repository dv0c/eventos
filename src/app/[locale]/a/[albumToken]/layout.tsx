import { mobileAppViewport } from "@/lib/mobile-app-viewport";

export const viewport = mobileAppViewport;

export default function AlbumTokenLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
